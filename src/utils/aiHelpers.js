// src/utils/aiHelpers.js

/**
 * Returns a category string based on the description.
 * Replace with real logic if you add merchant/category AI smartness later.
 * For now, always returns 'Others'.
 */
export function autoCategorizeMerchant(description) {
  // You can add smarter matching logic as needed!
  return 'Others';
}
